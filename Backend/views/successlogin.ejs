<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Welcome, <%= userName %>!</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://fonts.googleapis.com/css?family=Roboto:400,700&display=swap" rel="stylesheet">
  <style>
    body {
      background: linear-gradient(120deg, #43cea2 0%, #185a9d 100%);
      font-family: 'Roboto', sans-serif;
      min-height: 100vh;
      margin: 0;
      display: flex;
      align-items: center;
      justify-content: center;
    }
    .card {
      background: #fff;
      border-radius: 18px;
      box-shadow: 0 8px 32px rgba(60,60,60,0.18);
      padding: 2.5rem 2rem;
      max-width: 350px;
      width: 100%;
      text-align: center;
      animation: fadeIn 1s;
    }
    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(30px);}
      to { opacity: 1; transform: translateY(0);}
    }
    .profile-img {
      width: 100px;
      height: 100px;
      border-radius: 50%;
      object-fit: cover;
      border: 4px solid #43cea2;
      margin-bottom: 1rem;
      box-shadow: 0 2px 12px rgba(67,206,162,0.18);
      transition: transform 0.2s;
    }
    .profile-img:hover {
      transform: scale(1.07) rotate(-2deg);
    }
    h1 {
      color: #185a9d;
      margin-bottom: 0.5rem;
      font-size: 2rem;
      font-weight: 700;
    }
    p {
      color: #333;
      font-size: 1.1rem;
      margin-bottom: 1.5rem;
    }
    .logout-btn {
      display: inline-block;
      background: linear-gradient(90deg, #ff512f 0%, #dd2476 100%);
      color: #fff;
      padding: 0.7rem 1.5rem;
      border: none;
      border-radius: 6px;
      font-size: 1rem;
      font-weight: 700;
      text-decoration: none;
      transition: background 0.2s, transform 0.2s;
      box-shadow: 0 2px 8px rgba(221,36,118,0.12);
      cursor: pointer;
    }
    .logout-btn:hover {
      background: linear-gradient(90deg, #dd2476 0%, #ff512f 100%);
      transform: translateY(-2px) scale(1.03);
    }
    @media (max-width: 500px) {
      .card {
        padding: 1.2rem 0.5rem;
        max-width: 95vw;
      }
      h1 {
        font-size: 1.3rem;
      }
      .profile-img {
        width: 70px;
        height: 70px;
      }
    }

  .videoLink {
      display: inline-block;
      background: #43cea2;
      color: #fff;
      padding: 0.7rem 1.5rem;
      border-radius: 6px;
      font-size: 1rem;
      font-weight: 700;
      text-decoration: none;
      transition: background 0.2s, transform 0.2s;
      box-shadow: 0 2px 8px rgba(67,206,162,0.12);
    }
    .videoLink:hover {
      background: #185a9d;
      transform: translateY(-2px) scale(1.03);
    }
  </style>
</head>
<body>
  <div class="card">
    <img src="<%= profilePicture %>" alt="Profile Picture" class="profile-img">
    <h1>Welcome, <%= userName %>!</h1>
    <p>You have successfully logged in to the Video Transcoding Service.</p>
    <a href="/upload" class="videoLink">Click here to upload video</a>
    <a href="/logout" class="logout-btn">Logout</a>